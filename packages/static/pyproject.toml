[project]
name = "raya-static"
version = "0.1.0"
description = "Raya Lucaria Glintstone static artifact builder"
requires-python = ">=3.10"
dependencies = [
  "markdown-it-py>=3.0",
  "mdit-py-plugins>=0.4",
  "pygments>=2.18",
  "raya-schema",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/raya_static"]
