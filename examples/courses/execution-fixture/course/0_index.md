---
id: execution-root
title: Local Execution Fixture
summary: Fixture root for explicit local execution tests.
status: ready
---

# Local Execution Fixture

This is fixture material for local execution tests. It is not canonical pedagogy or architecture truth; use `docs/foundation/` for authority.

Inspect the [manual script](code/manual_task.py), [cache script](code/cache_task.py), [always script](code/always_task.py), [never script](code/never_task.py), [frozen script](code/frozen_task.py), [failing script](code/failing_task.py), and [demo notebook](notebooks/demo.ipynb).

Static validation and static builds may read these files, but they must not execute them.
